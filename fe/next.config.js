/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "tmdb.org",
            "themoviedb.org",
            "image.tmdb.org",
            "via.placeholder.com",
        ],
    },
};

module.exports = nextConfig;
